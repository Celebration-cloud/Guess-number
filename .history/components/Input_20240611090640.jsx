import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default function Input() {
    return <TextInput placeholder="Place Number" />
}

const styles = StyleSheet.create({

})
